# README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 利用者と開発者がプロジェクトの用途、基本操作、開発方法、Cloudflare Pages設定を短時間で理解できるREADMEを追加する。

**Architecture:** ルートの `README.md` を入口とし、日常的に必要な情報だけを掲載する。変換仕様とCAD検証の詳細は、既存の設計書およびチェックリストへの相対リンクで分離する。

**Tech Stack:** Markdown、Vite 8、Vitest 4、Cloudflare Pages

## Global Constraints

- READMEは利用者向けと開発者向けを兼ねた簡潔型とする。
- 変換はブラウザ内で完結し、入力データを外部APIへ送信しないことを明記する。
- 対応拡張子は `.hpgl`、`.hpg`、`.plt`、`.h01`〜`.h99` とする。
- 固定スケールは40 HPGL単位を1 mmとする。
- Cloudflare Pagesのビルドコマンドは `npm run build`、出力先は `dist` とする。
- スクリーンショット、対応コマンド完全一覧、DXF内部構造の詳細は掲載しない。

---

### Task 1: READMEの追加と検証

**Files:**
- Create: `README.md`
- Reference: `package.json`
- Reference: `vite.config.js`
- Reference: `docs/superpowers/specs/2026-07-14-hpgl-dxf-static-site-design.md`
- Reference: `docs/bricscad-v24-checklist.md`

**Interfaces:**
- Consumes: `package.json` のnpm scripts、Viteの `dist` 出力、既存ドキュメントの相対パス
- Produces: GitHubおよびローカルで表示できるプロジェクト案内 `README.md`

- [ ] **Step 1: READMEを作成する**

次の構成と内容で `README.md` を作成する。

```markdown
# HPGL → DXF Converter

複数のHPGLファイルを、ファイルごとのレイヤーを持つ1つのDXFへ変換する静的Webアプリです。変換処理はブラウザ内で完結し、入力ファイルを外部APIへ送信しません。

## 主な特徴

- 複数ファイルをまとめて1つのDXFへ変換
- 入力ファイルごとにDXFレイヤーを作成
- HPGLのペン番号をDXF図形のACI色として保持
- 40 HPGL単位を1 mmとして変換
- エラーのある入力をスキップし、変換できた図形を出力

## 対応ファイル

`.hpgl`、`.hpg`、`.plt`、`.h01`〜`.h99`

## 使い方

1. HPGLファイルを選択するか、画面へドラッグ＆ドロップします。
2. 必要に応じて出力ファイル名を変更します。
3. 「DXFに変換」を押します。
4. 結果を確認し、生成されたDXFをダウンロードします。

## ローカル開発

Node.js 22.12以降を使用してください。

```bash
npm ci
npm run dev
```

```bash
npm test
npm run build
```

ビルド結果は `dist` に生成されます。

## Cloudflare Pages

GitHubリポジトリをCloudflare Pagesへ接続し、次の値を設定します。

| 設定 | 値 |
|---|---|
| Production branch | `main` |
| Framework preset | `None` |
| Build command | `npm run build` |
| Build output directory | `dist` |

環境変数、Functions、外部APIは使用しません。

## ドキュメント

- [実装設計書](docs/superpowers/specs/2026-07-14-hpgl-dxf-static-site-design.md)
- [BricsCAD V24確認チェックリスト](docs/bricscad-v24-checklist.md)

## ライセンス

[MIT License](LICENSE)
```

- [ ] **Step 2: 記載内容とリンクを検証する**

Run:

```powershell
rg -n "外部API|\.hpgl|40 HPGL|npm ci|npm test|npm run build|Cloudflare Pages|dist" README.md
Test-Path 'docs\superpowers\specs\2026-07-14-hpgl-dxf-static-site-design.md'
Test-Path 'docs\bricscad-v24-checklist.md'
Test-Path 'LICENSE'
```

Expected: `rg` が各必須項目を表示し、3つの `Test-Path` がすべて `True` を返す。

- [ ] **Step 3: プロジェクトの回帰テストとビルドを実行する**

Run:

```powershell
npm test
npm run build
```

Expected: すべてのVitestテストが成功し、Viteが `dist/index.html` を生成する。

- [ ] **Step 4: READMEをコミットする**

```powershell
git add -- README.md
git commit -m "docs: add project readme"
```
